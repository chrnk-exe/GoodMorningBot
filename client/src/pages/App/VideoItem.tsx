import React from 'react';
import { Paper } from '@mui/material';

const VideoItem = ({content, id}: {content: string, id: number}) => {
	return (
		<Paper>VideoItem</Paper>
	);
};

export default VideoItem;